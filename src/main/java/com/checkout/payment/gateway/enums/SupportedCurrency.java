package com.checkout.payment.gateway.enums;

/**
 * Enum representing supported currencies for payment processing.
 * Each currency is represented by its standard three-letter code.
 * Allowed currencies are EUR, USD, and GBP.
 */
public enum SupportedCurrency {
  EUR,
  USD,
  GBP
}
